import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        
    },
    title: {
        fontSize: 40,
        maxWidth: '90%',
        textAlign: 'center',
  }
});

export default styles;